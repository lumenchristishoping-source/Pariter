# Virtual Storage — To-do

Tracking what's next so nothing gets lost between sessions.

- [ ] **File sharing** — Drew has a design already worked out, to be
      explained. Not the `secrets_api.py` `share()` method (that's just
      an access grant on one secret) — wait for the real spec before
      assuming it's the same thing.
- [ ] **Running multiple files** — testing the system handling several
      files at once (concurrently held, saved, retrieved together).

Update this list as items are explained, built, and verified.
